<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{subject}}</title>
    <style>
      body { font-family: Arial, sans-serif; background-color: #f4f4f4; margin:
      0; padding: 0; } .container { background-color: #ffffff; margin: 20px
      auto; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0, 0,
      0, 0.1); max-width: 600px; } h1 { color: #333333; } p { color: #555555;
      line-height: 1.6; } a { display: inline-block; margin-top: 20px; padding:
      10px 20px; color: #ffffff; background-color: #007bff; text-decoration:
      none; border-radius: 5px; } a:hover { background-color: #0056b3; } .note {
      margin-top: 20px; font-size: 0.9em; color: #777777; }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Hello, {{name}}!</h1>
      <p>The law firm of
        {{companyName}}
        is excited to have you on board. Please click the link below to verify
        your email address:</p>
      <a href="{{link}}">Verify Email</a>
      <p>Also, your temporary password upon successful verification is
        <strong>{{password}}</strong></p>
      <p class="note">Please change the password after logging into your
        account.</p>
      <p class="note">The link is valid for only 1 hour.</p>
      <p>Best regards,</p>
      <p>CaseMaster Team</p>
    </div>
  </body>
</html>