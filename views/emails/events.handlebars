<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event Notification - A.T. Lukman & Co.</title>
    <style>
      /* Optional styles for improved appearance */ body { font-family: Arial,
      sans-serif; background-color: #f4f4f4; margin: 0; padding: 0; } .container
      { width: 100%; max-width: 600px; margin: 0 auto; background-color:
      #ffffff; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0,
      0, 0, 0.1); } .header { text-align: center; padding: 10px 0;
      background-color: #007bff; color: #ffffff; border-radius: 8px 8px 0 0; }
      .content { padding: 20px; } .footer { text-align: center; padding: 10px 0;
      font-size: 12px; color: #777777; } table { width: 100%; } th, td {
      padding: 5px; border-bottom: 1px solid #ddd; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Event Notification - A.T. Lukman & Co.</h1>
      </div>
      <div class="content">
        <p>Dear {{#each participants}}{{this}},{{/each}}</p>
        <p>This email serves as a notification for an upcoming event.</p>
        <h2>{{title}}</h2>
        <p>**Organized by:** {{sendersName}} ({{sendersPosition}})</p>
        <table>
          <tbody>
            <tr>
              <th>Start Date:</th>
              <td>{{start}}</td>
            </tr>
            <tr>
              <th>End Date:</th>
              <td>{{end}}</td>
            </tr>
            <tr>
              <th>Location:</th>
              <td>{{location}}</td>
            </tr>
            <tr>
              <th>Description:</th>
              <td>{{description}}</td>
            </tr>
          </tbody>
        </table>
        <p>For more details and to RSVP, please visit:
          <a href="{{url}}">Event Details</a>.</p>
      </div>
      <div class="footer">
        <p>&copy; {{year}} A.T. Lukman & Co. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>